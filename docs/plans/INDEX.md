# Очередь задач

Единственный индекс приоритетов. Детали — в связанных файлах. Ветки не смешивать в одном PR/чате без явной задачи.

**Не делаем в текущей версии семантики:** переклассификация полей; чистка keywords; фильтр `/ontology/` и AI-поиска по `from_translation`.

## Сейчас

| # | Статус | Задача | Где |
|---|--------|--------|-----|
| 1 | в работе | Связи «от» / `ArticleLink.kind` (админка, подсветка) | `backlog.md` §1 |
| 2 | очередь | LLM-очистка переводов, затем проверка поиска | `backlog.md` §2, `docs/translation_cleanup.md` |
| 3 | блокер: §2 | Боевой прогон `from_translation` | `backlog.md` §4 |

## Дальше (продукт)

| # | Статус | Задача | Где |
|---|--------|--------|-----|
| 4 | спека | AI-поиск слой 1: разбор запроса + отдельная страница | `docs/ai-search.md` (клиент шлюза и `classify_article` уже есть; автохук после save — отдельно) |
| 5 | спека | Валидатор формы: сначала В2 (автокомплит «см.»), затем В1 и html↔структура | `docs/SPEC_article_form_validator.md` |
| 6 | бэклог | Поиск неразмеченных помет | `backlog.md` §3 |

## Отдельная ветка: лексический поиск

Не семантика и не AI. Пункты: `docs/backlog-search.md` (синонимия «связанных запросов», эталон «корова», стеммер, prio-фильтр «быть»). Открытые UX-пункты после cleanup — тоже там и в `backlog.md` §2 (TODO поиска).

## Сделано (не трогать без регрессии)

- `kind` на `ArticleLink`, инвентаризация, автосоздание однозначных `deriv` — `backlog.md` §1 шаги 1–4.
- Сужающие теги русского поиска `?f=`, `find_exact_match_ids` — `docs/searching_upgrade.md`, `docs/backlog-search.md`.
